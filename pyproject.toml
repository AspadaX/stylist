[project]
name = "stylist"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.28.1",
    "streamlit>=1.40.2",
]

[tool.pyright]
venv = ".venv"
venvPath = "."
